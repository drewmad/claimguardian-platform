{
  "installCommand": "pnpm install --no-frozen-lockfile",
  "buildCommand": "pnpm --filter=@claimguardian/ui build && pnpm --filter=@claimguardian/utils build && pnpm --filter=@claimguardian/db build && pnpm --filter=web build",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs",
  "github": {
    "silent": true
  },
  "env": {
    "SKIP_ENV_VALIDATION": "true",
    "FORCE_COLOR": "1"
  }
}
