# Database Credentials (Change in production!)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=storm_eoc

# Mapbox Access Token (Required for Frontend build/runtime)
# Ensure this is set if building frontend locally or via Docker build args
# VITE_MAPBOX_TOKEN=pk.YOUR_MAPBOX_TOKEN

# X/Twitter API Bearer Token (Required for Social Media Ingestion)
# X_BEARER_TOKEN=YOUR_X_BEARER_TOKEN

# Optional: Other API Keys
# NOAA_API_KEY=YOUR_NOAA_API_KEY
# PLANET_API_KEY=YOUR_PLANET_API_KEY
